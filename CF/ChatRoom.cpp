#include<iostream>
using namespace std;

int main(int argc, char const *argv[])
{
    string s;
    cin >> s;
    char prev = '\0';
    char curr = 'h';
    int j = 0;
    int i = 0;
    string target = "hello";
    while(j < s.size()) {
        if(s[j] == curr || s[j] == )
    }
    return 0;
}
